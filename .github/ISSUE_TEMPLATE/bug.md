---
name: Bug
about: A program that fails, answers differently on one of the three paths, or gets a wrong or unhelpful diagnostic
labels: bug
---

**The program.** The smallest `.ppy` (or `.py`) that shows it.

```python
```

**The command, and what it printed.** When the three paths disagree, all
three: `python f.ppy`, `ppy f.ppy`, `ppy run f.ppy`.

```text
```

**What you expected.**

**The machine.** Paste `ppy --version` and `ppy doctor`.

```text
```
