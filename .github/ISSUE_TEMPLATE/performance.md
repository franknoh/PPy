---
name: Performance
about: A program the native path should make faster and does not
labels: performance
---

**The program.**

```python
```

**The numbers.** `python f.ppy`, `ppy run f.ppy`, and a built binary if you
have one, each timed the same way; `examples/15_algorithms/bench.py --json`
records a run with the machine. What `ppy explain f.ppy:function` says about
the function is usually the answer to "why".

```text
```

**The machine.** `ppy doctor`.

```text
```
