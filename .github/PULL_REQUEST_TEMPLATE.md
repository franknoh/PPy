<!--
One change, into `dev` (into `main` only for a release or a fix that cannot wait).
Say what changed and why, in a few paragraphs a reader could reconstruct the
decision from. `Closes #N` links the issue. Commit lines are one lower-case
sentence each, no prefixes, no body.
-->

**What changed.**

**Why.**

- [ ] `./scripts/check.sh` passes
- [ ] `CHANGELOG.md` has a line under the unreleased section, if behavior changed
- [ ] examples and their recorded outputs are in step (`scripts/refresh.py --quick`, `examples/record_outputs.py <folder>`)
