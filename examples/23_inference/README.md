# How far inference reaches without a single annotation

Three modules with no type annotations at all, and the three `.ppy` files
`ppy convert` wrote from them. Each one exercises one thing the inference
has to get right — call-graph propagation, classes and optionals, and all
of it at once — and each converted file is regenerated by
`examples/verify_conversions.py` to prove it is exactly what the converter
produces.

## `stats.py`: types flow along the call graph

Nothing is annotated. `mean`, `variance`, `standardize`, and `report` all
get parameter and return types from one module-level `samples` list of
floats: the call site types `values`, the arithmetic types the return, and
the next call carries it on. `Sequence[float]` rather than `list[float]`
because every body only reads.

## `shapes.py`: fields from `__init__`, and `None` as a real case

```python
@ppy.pure
def widest(rects: Sequence[Rect]) -> Rect | None:
    best: Rect | None = None
    for rect in rects:
        if best is None or rect.width > best.width:
            best = rect
    return best
```

`Rect.width` and `Rect.height` are typed from what `__init__` assigns.
`widest` is defined before `Rect`, so its annotation is quoted as a forward
reference; it returns `Rect | None` because one path returns `best`'s
initial value, and `label` accepts that union and narrows it.

## `pipeline.py`: everything at once

A call chain several functions deep, instance fields from `__init__`, a
class used before it is defined, a list whose element type comes from what
is appended, `shifted` — which nothing calls — typed from the arithmetic in
its body, and `@ppy.pure` where the checker proved it. The converter moved
`Summary` above `summarize` because the move is provably safe; with
`--hoist-classes=off` it would have kept the quoted forward reference.

## What the converter will not do

Rename, split a function that does several jobs, or restructure an
algorithm. Those are design decisions. When a buffer promotion is blocked it
names the blocker rather than guessing:

```
remark[R3003]: `raw` could be a borrowed buffer, but `raw` is sliced, which
               copies; indexing it element by element instead would let the
               memory be borrowed
```

## Run it

```bash
ppy convert pipeline.py --dry-run
python  pipeline.py
ppy run pipeline.ppy
```

<!-- outputs:start -->
## What it prints

**`ppy convert pipeline.py --dry-run`**

```text
# ---- ./pipeline.ppy ----
import math
from collections.abc import Sequence

import ppy


class Summary:
    def __init__(self, mean: float, deviation: float, count: int) -> None:
        self.mean: float = mean
        self.deviation: float = deviation
        self.count: int = count

    def scaled(self, factor: float) -> 'Summary':
        return Summary(self.mean * factor, self.deviation * factor, self.count)

    def shifted(self, offset: float) -> 'Summary':
        return Summary(self.mean + offset, self.deviation, self.count)

    @ppy.pure
```

*74 lines in all — [full output](outputs/01-ppy-convert-pipeline-py-dry-run.txt).*

**`python  pipeline.py`**

```text
n=8 mean=5.000 sd=2.000 first=-1.5
empty
n=8 mean=10.000 sd=4.000
```

**`ppy run pipeline.ppy`**

```text
n=8 mean=5.000 sd=2.000 first=-1.5
empty
n=8 mean=10.000 sd=4.000
```

<!-- outputs:end -->

## Read on

- [Conversion and inference](../../docs/internals/conversion.md) — the fixpoint, protocol widening, `Final`, the strict gate.
- [Inventory](../20_inventory/README.md) — a converted module with buffers promoted.

Generated, not hand-written: `pipeline.ppy`, `shapes.ppy`, and `stats.ppy`
are exactly what `ppy convert` writes from the `.py` beside each, and
`examples/verify_conversions.py` checks that on every run.
