[project]
name = "ppy-example-multi-gpu-jax"
version = "0.1.0"
requires-python = ">=3.12"

[tool.ppy]
strict = true
opt-level = 3
source-roots = ["."]

[tool.ppy.plugins.jax]
enabled = true

[tool.ppy.diagnostics]
optimization-remarks = true
